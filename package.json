{
    "name": "ahost-bilisim",
    "version": "1.0.0",
    "description": "Ahost Bilişim — E2E test suite",
    "private": true,
    "scripts": {
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui",
        "test:e2e:report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.48.0"
    }
}
